// Os dados primitivos são string, number, ndefined, null, boolean e symbol

let numero1 = null; //Não apota para nenhum local na memoria
const aprovado = false; //Variaveis boolean (true / false)

const lista = [1, 2, 3];

lista.push(4);

console.log(lista);
